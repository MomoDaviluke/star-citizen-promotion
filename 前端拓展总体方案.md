# 星际公民战队站 - 前端拓展总体方案

> 目标：打造一个高可维护性 + 创意十足的星际公民战队网站
> 版本：v2.0
> 日期：2026-05-08

---

## 一、架构设计（可维护性基础）

### 1.1 目录结构规范

```
src/
├── components/
│   ├── common/          # 通用基础组件
│   │   ├── PageTitle.vue
│   │   ├── PageTransition.vue
│   │   ├── LoadingIndicator.vue
│   │   ├── ErrorBoundary.vue
│   │   ├── BaseButton.vue         # 新增：统一按钮组件
│   │   ├── BaseCard.vue          # 新增：统一卡片组件
│   │   ├── BaseBadge.vue        # 新增：统一徽章组件
│   │   ├── BaseModal.vue        # 新增：统一弹窗组件
│   │   └── BaseTooltip.vue     # 新增：统一提示组件
│   ├── layout/          # 布局组件
│   │   ├── SiteHeader.vue
│   │   ├── SiteFooter.vue
│   │   ├── Sidebar.vue         # 新增：侧边栏
│   │   └── Breadcrumb.vue      # 新增：面包屑导航
│   ├── fleet/           # 舰队模块组件
│   ├── calendar/        # 日历模块组件
│   ├── stats/          # 统计模块组件
│   ├── leaderboard/    # 排行榜模块组件
│   ├── galaxy/         # 星系地图组件
│   ├── media/          # 媒体墙组件
│   ├── wiki/           # Wiki模块组件
│   ├── chat/           # 聊天模块组件
│   └── effects/        # 视觉效果组件
│       ├── ParticleBackground.vue
│       ├── ScanLines.vue
│       ├── HologramEffect.vue
│       └── GlitchText.vue
├── views/
│   ├── admin/
│   ├── Home.vue
│   ├── About.vue
│   ├── Members.vue
│   ├── Projects.vue
│   ├── Fleet.vue           # 新增：舰队展示页
│   ├── Calendar.vue        # 新增：活动日历页
│   ├── Leaderboard.vue     # 新增：排行榜页
│   ├── GalaxyMap.vue      # 新增：星系地图页
│   ├── Media.vue          # 新增：媒体展示页
│   ├── Wiki.vue           # 新增：新手指南页
│   ├── Community.vue      # 新增：社区动态页
│   └── Donate.vue        # 新增：赞助展示页
├── composables/      # 新增：组合式函数
│   ├── useAnimation.js
│   ├── useParallax.js
│   ├── useScrollReveal.js
│   ├── useSoundEffect.js  # 音效管理
│   └── useWebSocket.js    # WebSocket管理
├── stores/            # 新增：状态管理（Pinia）
│   ├── useAuthStore.js
│   ├── useFleetStore.js
│   ├── useCalendarStore.js
│   ├── useStatsStore.js
│   └── useChatStore.js
├── styles/            # 新增：样式系统
│   ├── variables.css        # CSS变量（主题系统）
│   ├── animations.css       # 动画关键帧
│   ├── effects.css          # 特效样式
│   └── components/          # 组件样式
├── utils/             # 新增：工具函数
│   ├── formatters.js        # 数据格式化
│   ├── validators.js        # 表单验证
│   └── soundEffects.js     # 音效工具
└── services/
    ├── http.js
    ├── dataService.js
    ├── aiService.js
    ├── authService.js
    ├── fleetService.js      # 新增
    ├── calendarService.js   # 新增
    ├── statsService.js      # 新增
    └── websocketService.js  # 新增
```

### 1.2 技术栈升级

```json
{
  "dependencies": {
    "vue": "^3.4",
    "vue-router": "^4.3",
    "pinia": "^2.1",              // 状态管理
    "chart.js": "^4.4",           // 数据可视化
    "swiper": "^11.0",            // 轮播组件
    "particles.vue3": "^2.12",    // 粒子效果
    "fullcalendar": "^6.1",       // 日历
    "photoswipe": "^5.4",         // 图片灯箱
    "markdown-it": "^14.0",       // Markdown渲染
    "animejs": "^3.2",            // 动画库
    "howler": "^2.2",             // 音效管理
    "gsap": "^3.12"               // 高级动画
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0",
    "sass": "^1.71",              // SCSS预处理器
    "eslint": "^8.56",
    "vitest": "^1.2",
    "@vue/test-utils": "^2.4"
  }
}
```

### 1.3 状态管理规范（Pinia Store结构）

```javascript
// stores/useFleetStore.js 示例
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { fleetService } from '@/services/fleetService'

export const useFleetStore = defineStore('fleet', () => {
  // 状态
  const ships = ref([])
  const loading = ref(false)
  const filter = ref('all') // all|combat|transport|explore

  // 计算属性
  const filteredShips = computed(() => {
    if (filter.value === 'all') return ships.value
    return ships.value.filter(ship => ship.category === filter.value)
  })

  const totalValue = computed(() => 
    ships.value.reduce((sum, ship) => sum + ship.value, 0)
  )

  // 方法
  async function fetchShips() {
    loading.value = true
    try {
      ships.value = await fleetService.getFleet()
    } finally {
      loading.value = false
    }
  }

  function setFilter(newFilter) {
    filter.value = newFilter
  }

  return {
    ships,
    loading,
    filter,
    filteredShips,
    totalValue,
    fetchShips,
    setFilter
  }
})
```

---

## 二、创意设计系统

### 2.1 主题变量系统（CSS Variables）

```css
/* styles/variables.css */
:root {
  /* 主色调 - 科技蓝 */
  --color-primary: #5fa9ff;
  --color-primary-dark: #3d7bd9;
  --color-primary-light: #8fc7ff;
  --color-accent: #00d4ff;
  --color-accent-2: #4ecdc4;

  /* 背景色阶 */
  --bg-deepest: #030810;
  --bg-deep: #0a1620;
  --bg-medium: #152030;
  --bg-light: #1e3050;
  --bg-overlay: rgba(10, 22, 40, 0.95);

  /* 文字颜色 */
  --text-primary: #e0e8f0;
  --text-secondary: #8fa8c0;
  --text-muted: #5a7a9a;
  --text-accent: #5fa9ff;

  /* 边框/分割线 */
  --border-subtle: rgba(95, 169, 255, 0.15);
  --border-medium: rgba(95, 169, 255, 0.3);
  --border-strong: rgba(95, 169, 255, 0.5);

  /* 阴影/光晕 */
  --glow-primary: 0 0 20px rgba(95, 169, 255, 0.3);
  --glow-accent: 0 0 15px rgba(0, 212, 255, 0.25);
  --glow-strong: 0 0 30px rgba(95, 169, 255, 0.5);

  /* 动画时长 */
  --transition-fast: 150ms ease;
  --transition-normal: 300ms ease;
  --transition-slow: 500ms ease;

  /* 间距比例 */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 1rem;
  --space-lg: 1.5rem;
  --space-xl: 2rem;
  --space-2xl: 3rem;
}

/* 亮色主题 */
[data-theme="light"] {
  --bg-deepest: #f0f4f8;
  --bg-deep: #e2e8f0;
  --bg-medium: #ffffff;
  --bg-light: #f8fafc;
  --text-primary: #1a202c;
  --text-secondary: #4a5568;
  --text-muted: #718096;
  --border-subtle: rgba(0, 0, 0, 0.1);
}
```

### 2.2 动画关键帧库

```css
/* styles/animations.css */

/* 扫描线效果 */
@keyframes scanLine {
  0% { transform: translateX(-100%); opacity: 0; }
  50% { opacity: 1; }
  100% { transform: translateX(100%); opacity: 0; }
}

/* 光晕脉冲 */
@keyframes pulseGlow {
  0%, 100% { box-shadow: 0 0 5px var(--color-primary); }
  50% { box-shadow: 0 0 20px var(--color-primary), 0 0 40px rgba(95, 169, 255, 0.3); }
}

/* 数字滚动 */
@keyframes countUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* 全息效果 */
@keyframes hologramFlicker {
  0%, 100% { opacity: 0.95; }
  50% { opacity: 0.8; }
  75% { opacity: 0.9; }
}

/* 故障效果 */
@keyframes glitch {
  0% { transform: translate(0); }
  20% { transform: translate(-2px, 2px); }
  40% { transform: translate(-2px, -2px); }
  60% { transform: translate(2px, 2px); }
  80% { transform: translate(2px, -2px); }
  100% { transform: translate(0); }
}

/* 渐入向上 */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

/* 渐入缩放 */
@keyframes fadeInScale {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}
```

### 2.3 创意UI组件规范

**科幻风格卡片**
```vue
<!-- components/common/BaseCard.vue -->
<template>
  <div class="sci-fi-card" :class="{ 'is-hoverable': hoverable }">
    <div class="card-border-effect"></div>
    <div class="card-content">
      <slot />
    </div>
    <div class="card-corner card-corner-tl"></div>
    <div class="card-corner card-corner-tr"></div>
    <div class="card-corner card-corner-bl"></div>
    <div class="card-corner card-corner-br"></div>
  </div>
</template>

<style scoped>
.sci-fi-card {
  position: relative;
  padding: 1.5rem;
  background: linear-gradient(165deg, rgba(15, 30, 50, 0.8), rgba(8, 18, 32, 0.9));
  border: 1px solid var(--border-subtle);
  border-radius: 4px;
  overflow: hidden;
  transition: all var(--transition-normal);
}

.sci-fi-card.is-hoverable:hover {
  border-color: var(--border-medium);
  box-shadow: var(--glow-primary);
  transform: translateY(-4px);
}

.card-border-effect {
  position: absolute;
  inset: 0;
  background: linear-gradient(90deg, transparent, var(--color-primary), transparent);
  opacity: 0;
  transition: opacity var(--transition-normal);
  mask-image: linear-gradient(90deg, transparent 0%, black 50%, transparent 100%);
  pointer-events: none;
}

.sci-fi-card:hover .card-border-effect {
  opacity: 0.1;
}

.card-corner {
  position: absolute;
  width: 12px;
  height: 12px;
  border-color: var(--color-primary);
  opacity: 0.5;
  transition: opacity var(--transition-fast);
}

.sci-fi-card:hover .card-corner { opacity: 1; }

.card-corner-tl { top: 8px; left: 8px; border-width: 2px 0 0 2px; border-style: solid; }
.card-corner-tr { top: 8px; right: 8px; border-width: 2px 2px 0 0; border-style: solid; }
.card-corner-bl { bottom: 8px; left: 8px; border-width: 0 0 2px 2px; border-style: solid; }
.card-corner-br { bottom: 8px; right: 8px; border-width: 0 2px 2px 0; border-style: solid; }
</style>
```

---

## 三、十大功能模块详细实现

### 模块1：舰队展示（Fleet Showcase）

**功能列表：**
- 飞船3D卡片翻转效果
- 按类别筛选（战斗/运输/探索）
- 飞船状态标签（在库/出借/战斗中）
- 飞船详情弹窗
- 舰队总价值计算

**创意设计：**
- 卡片悬停时显示全息投影效果
- 飞船图片添加扫描线动画
- 使用Swiper.js实现3D轮播

**组件结构：**
```
components/fleet/
├── FleetGrid.vue          # 舰队网格布局
├── ShipCard.vue          # 飞船卡片（翻转效果）
├── ShipFilter.vue        # 筛选器
├── ShipDetail.vue        # 详情弹窗
└── FleetStats.vue        # 舰队统计
```

---

### 模块2：活动日历（Event Calendar）

**功能列表：**
- 月历/周历/列表视图切换
- 活动创建（管理员）
- 活动报名系统
- 浏览器通知提醒
- ICS日历文件导出

**创意设计：**
- 日期格子添加科幻边框
- 活动条目添加脉冲动画
- 今天日期特殊光晕效果

**组件结构：**
```
components/calendar/
├── CalendarView.vue      # 日历主视图
├── CalendarHeader.vue    # 月份切换头部
├── MonthView.vue        # 月历视图
├── ListView.vue         # 列表视图
├── EventCard.vue        # 活动卡片
└── EventModal.vue       # 活动详情弹窗
```

---

### 模块3：实时数据统计（Live Stats Dashboard）

**功能列表：**
- 战队KPI数字滚动动画
- 击杀/死亡比图表
- 任务完成趋势图
- 赚取UEC总量
- 航行总里程

**创意设计：**
- 数字滚动时添加咖喱噪点效果
- 图表使用科技感配色
- 添加实时更新脉冲指示器

**组件结构：**
```
components/stats/
├── StatsDashboard.vue   # 统计面板主页
├── StatCard.vue         # 统计卡片（数字动画）
├── KillDeathChart.vue   # K/D比图表
├── MissionChart.vue     # 任务趋势图
└── RealtimeIndicator.vue # 实时更新指示器
```

---

### 模块4：成员贡献排行榜（Leaderboard）

**功能列表：**
- 多维度排序（击杀/任务/在线时长）
- 时间段筛选（周/月/总榜）
- 勋章成就展示
- 排名变化箭头指示

**创意设计：**
- 前三名特殊光晕效果
- 排名数字使用渐变文字
- 勋章添加旋转动画

**组件结构：**
```
components/leaderboard/
├── LeaderboardTable.vue # 排行榜表格
├── LeaderboardFilter.vue # 筛选器
├── MemberRank.vue       # 成员排名行
└── AchievementBadge.vue # 勋章组件
```

---

### 模块5：星系地图探索（Galaxy Map）

**功能列表：**
- SVG交互式星图
- 地点标记（空间站/危险区/资源点）
- 点击标记显示详情
- 缩放和平移功能

**创意设计：**
- 星空背景添加闪烁动画
- 航线添加流动光效
- 标记点添加脉冲波纹

**组件结构：**
```
components/galaxy/
├── GalaxyMap.vue        # 星系地图主组件
├── StarSystem.vue       # 恒星系组件
├── MapMarker.vue        # 地图标记
├── RouteLine.vue        # 航线组件
└── MapTooltip.vue       # 标记提示
```

---

### 模块6：媒体展示墙（Media Gallery）

**功能列表：**
- 瀑布流布局
- 分类筛选（风景/战斗/活动）
- 灯箱大图查看
- 视频嵌入播放

**创意设计：**
- 图片加载时添加扫描线效果
- 灯箱背景添加粒子效果
- 视频封面添加播放按钮光晕

**组件结构：**
```
components/media/
├── MediaGallery.vue     # 媒体墙主页
├── ImageGrid.vue        # 图片瀑布流
├── VideoGrid.vue        # 视频网格
├── MediaFilter.vue      # 分类筛选
└── LightboxViewer.vue   # 灯箱查看器
```

---

### 模块7：新手指南Wiki（Wiki/Guide）

**功能列表：**
- Markdown渲染
- 侧边栏目录导航
- 搜索功能
- 阅读进度条

**创意设计：**
- 代码块添加科幻边框
- 标题添加装饰线
- 侧边栏添加展开动画

**组件结构：**
```
components/wiki/
├── WikiPage.vue         # Wiki主页
├── WikiSidebar.vue      # 侧边栏导航
├── WikiContent.vue      # 内容渲染区
├── WikiSearch.vue       # 搜索框
└── MarkdownRenderer.vue # Markdown渲染器
```

---

### 模块8：社区动态（Community Feed）

**功能列表：**
- 实时动态流
- 点赞/评论功能
- @提及功能
- 表情反应

**创意设计：**
- 动态条目添加滑入动画
- 点赞按钮添加粒子爆发效果
- 新消息提示添加闪烁边框

**组件结构：**
```
components/chat/
├── CommunityFeed.vue    # 社区动态流
├── FeedItem.vue         # 动态条目
├── CommentBox.vue       # 评论框
└── RealtimeBadge.vue    # 实时状态徽章
```

---

### 模块9：赞助/捐赠展示（Donation Tracker）

**功能列表：**
- 筹款进度条
- 赞助者名单展示
- 赞助等级显示
- 目标达成庆祝动画

**创意设计：**
- 进度条添加流动光效
- 赞助者头像添加边框光晕
- 达成目标时触发全屏庆祝动画

**组件结构：**
```
views/
└── Donate.vue           # 赞助展示页
components/
└── donate/
    ├── ProgressTracker.vue # 进度追踪
    ├── SponsorList.vue     # 赞助者列表
    └── DonateButton.vue   # 捐赠按钮
```

---

### 模块10：全局视觉效果系统

**功能列表：**
- 粒子背景
- 扫描线覆盖层
- 鼠标跟随光晕
- 页面切换全息效果
- 音效系统（按钮点击/页面切换）

**创意设计：**
- 粒子颜色随主题变化
- 扫描线速度可调
- 光晕效果使用CSS blend mode

**组件结构：**
```
components/effects/
├── ParticleBackground.vue  # 粒子背景
├── ScanLines.vue           # 扫描线
├── MouseGlow.vue           # 鼠标光晕
├── HologramTransition.vue  # 全息过渡
└── SoundEffect.vue         # 音效控制器
```

---

## 四、实施路线图

### 第一阶段（第1-2周）：基础架构搭建

**任务：**
1. 升级技术栈（安装新依赖）
2. 建立目录结构
3. 配置Pinia状态管理
4. 创建基础组件库（BaseButton, BaseCard等）
5. 建立CSS变量主题系统
6. 配置动画关键帧库

**交付物：**
- 可运行的基础架构
- 组件库文档
- 主题切换功能

---

### 第二阶段（第3-4周）：核心功能模块

**任务：**
1. 实现舰队展示模块
2. 实现活动日历模块
3. 实现实时数据统计面板
4. 实现成员排行榜

**交付物：**
- 4个完整功能模块
- 单元测试覆盖率 > 70%
- 响应式适配完成

---

### 第三阶段（第5-6周）：交互体验模块

**任务：**
1. 实现星系地图探索
2. 实现媒体展示墙
3. 实现全局视觉效果系统
4. 实现音效系统

**交付物：**
- 3个完整功能模块
- 视觉效果性能优化
- 音效用户体验测试

---

### 第四阶段（第7-8周）：社区功能模块

**任务：**
1. 实现新手指南Wiki
2. 实现社区动态Feed
3. 实现赞助/捐赠展示
4. 全站SEO优化

**交付物：**
- 3个完整功能模块
- SEO评分 > 90
- 全站链接检查

---

### 第五阶段（第9-10周）：测试与优化

**任务：**
1. 端到端测试（E2E）
2. 性能优化（Lighthouse > 90）
3. 浏览器兼容性测试
4. 安全审计
5. 文档编写

**交付物：**
- 测试报告
- 性能报告
- 部署文档
- 用户手册

---

## 五、代码质量标准

### 5.1 组件规范

```vue
<!-- 组件模板规范示例 -->
<template>
  <div class="component-name" :class="{ 'is-active': active }">
    <!-- 使用v-if instead of v-show for conditional rendering when possible -->
    <header v-if="showHeader">
      <slot name="header" />
    </header>
    
    <main>
      <slot />
    </main>
    
    <footer v-if="$slots.footer">
      <slot name="footer" />
    </footer>
  </div>
</template>

<script setup>
/**
 * 组件名称
 * @description 组件的详细功能描述
 * @props {String} propName - prop说明
 * @slots header - 头部插槽
 * @slots default - 默认插槽
 * @slots footer - 底部插槽
 * @events click - 点击事件
 */

import { ref, computed, onMounted, onUnmounted } from 'vue'
import { useSomeStore } from '@/stores'

// Props定义（使用TypeScript风格注释）
const props = defineProps({
  title: {
    type: String,
    required: true
  },
  variant: {
    type: String,
    default: 'default',
    validator: (value) => ['default', 'primary', 'secondary'].includes(value)
  }
})

// Emits定义
const emit = defineEmits(['click', 'update:modelValue'])

// 状态
const isActive = ref(false)

// 计算属性
const classes = computed(() => [
  'component-name',
  `component-name--${props.variant}`,
  { 'is-active': isActive.value }
])

// 方法
function handleClick() {
  isActive.value = !isActive.value
  emit('click', isActive.value)
}

// 生命周期
onMounted(() => {
  // 初始化逻辑
})

onUnmounted(() => {
  // 清理逻辑
})
</script>

<style scoped>
/**
 * 样式规范：
 * 1. 使用BEM命名法
 * 2. 避免深层嵌套（最多3层）
 * 3. 使用CSS变量而非硬编码颜色
 * 4. 添加响应式媒体查询
 */
.component-name {
  /* 布局 */
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
  
  /* 外观 */
  background: var(--bg-medium);
  border: 1px solid var(--border-subtle);
  border-radius: 4px;
  padding: var(--space-lg);
  
  /* 动画 */
  transition: all var(--transition-normal);
}

.component-name--primary {
  border-color: var(--color-primary);
}

.is-active {
  box-shadow: var(--glow-primary);
}

/* 响应式 */
@media (max-width: 768px) {
  .component-name {
    padding: var(--space-md);
  }
}
</style>
```

### 5.2 服务层规范

```javascript
// services/fleetService.js 示例
/**
 * 舰队服务模块
 * @module fleetService
 * @description 封装所有舰队相关的API调用
 */

import httpClient from './http.js'

const BASE_URL = '/api/fleet'

export const fleetService = {
  /**
   * 获取舰队列表
   * @param {Object} params - 查询参数
   * @param {string} [params.category] - 飞船类别筛选
   * @param {string} [params.status] - 状态筛选
   * @returns {Promise<Array>} 飞船列表
   */
  async getFleet(params = {}) {
    const response = await httpClient.get(BASE_URL, { params })
    return response.data
  },

  /**
   * 获取单艘飞船详情
   * @param {string} shipId - 飞船ID
   * @returns {Promise<Object>} 飞船详情
   */
  async getShip(shipId) {
    if (!shipId) throw new Error('shipId is required')
    const response = await httpClient.get(`${BASE_URL}/${shipId}`)
    return response.data
  },

  /**
   * 创建飞船记录（管理员）
   * @param {Object} shipData - 飞船数据
   * @returns {Promise<Object>} 创建的飞船
   */
  async createShip(shipData) {
    const response = await httpClient.post(BASE_URL, shipData)
    return response.data
  },

  /**
   * 更新飞船信息
   * @param {string} shipId - 飞船ID
   * @param {Object} updates - 更新数据
   * @returns {Promise<Object>} 更新后的飞船
   */
  async updateShip(shipId, updates) {
    const response = await httpClient.patch(`${BASE_URL}/${shipId}`, updates)
    return response.data
  },

  /**
   * 删除飞船记录
   * @param {string} shipId - 飞船ID
   * @returns {Promise<void>}
   */
  async deleteShip(shipId) {
    await httpClient.delete(`${BASE_URL}/${shipId}`)
  }
}
```

### 5.3 组合式函数规范

```javascript
// composables/useScrollReveal.js 示例
/**
 * 滚动揭示动画组合式函数
 * @module useScrollReveal
 * @description 当元素进入视口时触发入场动画
 */

import { ref, onMounted, onUnmounted } from 'vue'

/**
 * 使用滚动揭示动画
 * @param {Object} options - 配置选项
 * @param {string} [options.animation='fadeInUp'] - 动画类型
 * @param {number} [options.threshold=0.1] - 触发阈值
 * @param {string} [options.rootMargin='0px'] - 根边距
 * @returns {Object} 暴露的响应式状态和函数
 */
export function useScrollReveal(options = {}) {
  const {
    animation = 'fadeInUp',
    threshold = 0.1,
    rootMargin = '0px'
  } = options

  const isVisible = ref(false)
  let observer = null

  /**
   * 初始化Intersection Observer
   * @param {HTMLElement} element - 目标元素
   */
  function initObserver(element) {
    if (!element) return

    observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            isVisible.value = true
            // 只触发一次，触发后断开观察
            observer.unobserve(entry.target)
          }
        })
      },
      { threshold, rootMargin }
    )

    observer.observe(element)
  }

  /**
   * 销毁观察器
   */
  function destroyObserver() {
    if (observer) {
      observer.disconnect()
      observer = null
    }
  }

  onMounted(() => {
    // 组件挂载后初始化
  })

  onUnmounted(() => {
    destroyObserver()
  })

  return {
    isVisible,
    initObserver,
    destroyObserver
  }
}
```

---

## 六、性能优化策略

### 6.1 代码分割

```javascript
// router/index.js 路由懒加载（已部分实现，需扩展）
const routes = [
  {
    path: '/fleet',
    component: () => import(/* webpackChunkName: "fleet" */ '@/views/Fleet.vue')
  },
  {
    path: '/calendar',
    component: () => import(/* webpackChunkName: "calendar" */ '@/views/Calendar.vue')
  },
  // ... 其他路由
]
```

### 6.2 图片优化

```vue
<!-- 图片懒加载组件 -->
<template>
  <img
    ref="imgRef"
    :src="src"
    :alt="alt"
    loading="lazy"
    @load="onLoad"
    class="lazy-image"
    :class="{ 'is-loaded': loaded }"
  />
</template>

<script setup>
import { ref } from 'vue'

const props = defineProps({
  src: String,
  alt: String
})

const imgRef = ref(null)
const loaded = ref(false)

function onLoad() {
  loaded.value = true
}
</script>

<style scoped>
.lazy-image {
  opacity: 0;
  transition: opacity 0.3s ease;
}

.lazy-image.is-loaded {
  opacity: 1;
}
</style>
```

### 6.3 虚拟滚动（长列表优化）

```vue
<!-- 虚拟滚动列表组件 -->
<template>
  <div class="virtual-list" ref="containerRef" @scroll="onScroll">
    <div class="virtual-list__phantom" :style="{ height: totalHeight + 'px' }"></div>
    <div class="virtual-list__content" :style="{ transform: `translateY(${offset}px)` }">
      <div
        v-for="item in visibleItems"
        :key="item.id"
        class="virtual-list__item"
      >
        <slot :item="item" />
      </div>
    </div>
  </div>
</template>
```

---

## 七、测试策略

### 7.1 单元测试（Vitest）

```javascript
// components/__tests__/BaseCard.spec.js
import { mount } from '@vue/test-utils'
import { describe, it, expect } from 'vitest'
import BaseCard from '@/components/common/BaseCard.vue'

describe('BaseCard.vue', () => {
  it('renders slot content', () => {
    const wrapper = mount(BaseCard, {
      slots: { default: '<div class="test">Content</div>' }
    })
    expect(wrapper.find('.test').exists()).toBe(true)
  })

  it('applies hover class when hoverable prop is true', () => {
    const wrapper = mount(BaseCard, { props: { hoverable: true } })
    expect(wrapper.classes()).toContain('is-hoverable')
  })
})
```

### 7.2 E2E测试（Playwright）

```javascript
// e2e/fleet.spec.js
import { test, expect } from '@playwright/test'

test.describe('Fleet Page', () => {
  test('should display fleet grid', async ({ page }) => {
    await page.goto('http://localhost:5173/fleet')
    await expect(page.locator('.fleet-grid')).toBeVisible()
  })

  test('should filter ships by category', async ({ page }) => {
    await page.goto('http://localhost:5173/fleet')
    await page.click('button:has-text("Combat")')
    const cards = await page.locator('.ship-card').count()
    expect(cards).toBeGreaterThan(0)
  })
})
```

---

## 八、部署与监控

### 8.1 构建优化

```javascript
// vite.config.js 生产构建优化
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vue-vendor': ['vue', 'vue-router', 'pinia'],
          'chart-vendor': ['chart.js'],
          'swiper-vendor': ['swiper']
        }
      }
    },
    chunkSizeWarningLimit: 1000
  }
})
```

### 8.2 性能监控

```javascript
// utils/performance.js
/**
 * 性能监控工具
 * @module performance
 */

export function measurePerformance() {
  if ('PerformanceObserver' in window) {
    // 监控LCP
    new PerformanceObserver((list) => {
      const entries = list.getEntries()
      const lastEntry = entries[entries.length - 1]
      console.log('LCP:', lastEntry.renderTime || lastEntry.loadTime)
    }).observe({ type: 'largest-contentful-paint' })

    // 监控CLS
    new PerformanceObserver((list) => {
      const entries = list.getEntries()
      entries.forEach((entry) => {
        if (!entry.hadRecentInput) {
          console.log('CLS:', entry.value)
        }
      })
    }).observe({ type: 'layout-shift' })
  }
}
```

---

## 九、项目交付清单

### 开发阶段交付物
- [ ] 完整源代码（含注释）
- [ ] 单元测试报告（覆盖率 > 70%）
- [ ] E2E测试报告
- [ ] 性能测试报告（Lighthouse > 90）
- [ ] 代码审查报告

### 文档交付物
- [ ] 组件库文档（Storybook）
- [ ] API接口文档
- [ ] 部署手册
- [ ] 用户使用手册
- [ ] 维护指南

### 设计交付物
- [ ] UI/UX设计稿（Figma）
- [ ] 图标资源包（SVG）
- [ ] 动画效果演示视频

---

## 十、风险评估与应对

| 风险 | 影响 | 应对措施 |
|------|------|----------|
| 第三方库兼容性问题 | 高 | 锁定版本号，编写兼容层 |
| 性能瓶颈（动画卡顿） | 中 | 使用requestAnimationFrame，降级策略 |
| 浏览器兼容性 | 中 | 添加polyfill，优雅降级 |
| 状态管理复杂度 | 高 | 严格遵循Pinia规范，模块化设计 |

---

## 总结

本方案提供：
- **高可维护性**：清晰的架构、严格的代码规范、完整的测试覆盖
- **创意十足**：科幻风格视觉效果、流畅动画、独特交互体验
- **完整功能**：十大功能模块，覆盖战队网站所有需求
- **专业交付**：详细的文档、测试、部署方案

**预计总时间：10周（60个工作日）**
**建议团队配置：1名前端负责人 + 1名UI设计师 + 1名测试工程师**
