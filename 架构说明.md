# 星际公民战队站 - 高拓展性架构说明

## 版本信息
- 版本：2.0.0
- 更新时间：2026-05-09
- 作者：全栈开发团队

## 架构设计原则

### 1. 模块化设计
- **按功能拆分**：每个功能模块独立成组件/视图/Store/服务
- **单一职责**：每个文件只负责一个明确的功能
- **高内聚低耦合**：模块间通过清晰的接口通信

### 2. 状态管理（Pinia）
```
src/stores/
├── auth.js          # 认证状态
├── fleet.js         # 舰队数据
├── calendar.js      # 活动日历
├── stats.js         # 统计数据
├── leaderboard.js   # 排行榜
├── galaxy.js        # 星系地图
├── media.js         # 媒体展示
├── wiki.js          # 新手指南
├── chat.js          # 社区动态
└── donate.js        # 赞助展示
```

**优势**：
- 类型安全（TypeScript 友好）
-  DevTools 支持
- 插件系统
- 模块化导入

### 3. 服务层分离
```
src/services/
├── index.js         # 统一导出
├── http.js          # HTTP 客户端
├── authService.js   # 认证服务
├── fleetService.js  # 舰队服务
├── calendarService.js # 日历服务
├── AIService.js    # AI 服务
└── ...
```

**设计模式**：
- Repository 模式：封装数据访问逻辑
- Factory 模式：创建复杂对象
- Singleton 模式：确保服务实例唯一

### 4. 组合式函数（Composables）
```
src/composables/
├── useScrollReveal.js   # 滚动揭示动画
├── useParallax.js       # 视差滚动
├── useSoundEffect.js    # 音效管理
├── useWebSocket.js      # WebSocket 管理
├── useTheme.js         # 主题切换
└── useBreakpoint.js    # 响应式断点
```

**优势**：
- 逻辑复用
- 可测试性
- 类型推导

### 5. 组件设计系统

#### 5.1 基础组件（Base Components）
```
src/components/common/
├── BaseButton.vue      # 统一按钮
├── BaseCard.vue        # 统一卡片
├── BaseModal.vue       # 统一弹窗
├── BaseBadge.vue       # 统一徽章
├── BaseTooltip.vue     # 统一提示
└── PageTitle.vue       # 页面标题
```

**设计原则**：
- 单一职责
- 可配置性（props）
- 事件驱动（emit）
- 插槽支持（slots）

#### 5.2 业务组件（Feature Components）
```
src/components/
├── fleet/        # 舰队相关
├── calendar/     # 日历相关
├── stats/        # 统计相关
├── leaderboard/  # 排行榜相关
├── galaxy/       # 星系地图相关
├── media/        # 媒体相关
├── wiki/         # Wiki相关
├── chat/         # 聊天相关
└── effects/      # 视觉效果相关
```

### 6. 样式系统

#### 6.1 CSS 变量主题
```css
/* src/styles/variables.css */
:root {
  /* 颜色系统 */
  --primary: #5fa9ff;
  --accent: #00d4ff;
  /* 间距系统 */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  /* 字体系统 */
  --font-sans: 'Inter', system-ui, sans-serif;
  /* 动画系统 */
  --transition-fast: 150ms ease;
  --transition-normal: 300ms ease;
}
```

#### 6.2 动画库
```css
/* src/styles/animations.css */
@keyframes scanline {
  /* 扫描线效果 */
}
@keyframes glitch {
  /* 故障效果 */
}
```

### 7. 路由设计
```javascript
// src/router/index.js
{
  path: '/fleet',
  name: 'Fleet',
  component: () => import('../views/Fleet.vue'), // 懒加载
  meta: {
    title: '舰队展示',
    preload: false,
    requiresAuth: true
  }
}
```

**优化策略**：
- 懒加载（Lazy Loading）
- 预加载（Preload）
- 路由守卫（Guards）

### 8. 性能优化

#### 8.1 代码分割
```javascript
// vite.config.js
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vue: ['vue', 'vue-router', 'pinia']
      }
    }
  }
}
```

#### 8.2 图片优化
- 懒加载（Lazy Load）
- 响应式图片（Responsive Images）
- WebP 格式

#### 8.3 缓存策略
- HTTP 缓存
- Service Worker
- LocalStorage

## 拓展性指南

### 1. 添加新功能模块

**步骤**：
1. 创建 Store：`src/stores/xxx.js`
2. 创建服务：`src/services/xxxService.js`
3. 创建视图：`src/views/Xxx.vue`
4. 创建组件：`src/components/xxx/`
5. 添加路由：`src/router/index.js`
6. 更新导航：`src/data/siteContent.js`

**示例**：添加"任务系统"
```bash
# 1. 创建 Store
touch src/stores/mission.js

# 2. 创建服务
touch src/services/missionService.js

# 3. 创建视图
touch src/views/Mission.vue

# 4. 创建组件
mkdir src/components/mission
touch src/components/mission/MissionCard.vue
touch src/components/mission/MissionList.vue

# 5. 添加路由
# 编辑 src/router/index.js

# 6. 更新导航
# 编辑 src/data/siteContent.js
```

### 2. 添加新主题

**步骤**：
1. 在 `src/styles/variables.css` 添加新主题变量
2. 在 `src/composables/useTheme.js` 添加主题切换逻辑
3. 在组件中使用 CSS 变量

**示例**：添加"暗黑主题"
```css
/* src/styles/variables.css */
[data-theme="dark"] {
  --bg-primary: #000000;
  --text-primary: #ffffff;
}
```

### 3. 添加新动画效果

**步骤**：
1. 在 `src/styles/animations.css` 添加新动画
2. 在组件中使用动画

**示例**：添加"淡入上升"动画
```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in-up {
  animation: fadeInUp var(--transition-normal);
}
```

## 最佳实践

### 1. 代码规范
- 使用 ESLint + Prettier
- 遵循 Vue 风格指南
- 使用 JSDoc 注释

### 2. 提交规范
```
feat: 添加舰队展示模块
fix: 修复登录页面错误
docs: 更新API文档
style: 格式化代码
refactor: 重构认证模块
test: 添加单元测试
chore: 更新依赖
```

### 3. 测试策略
- 单元测试：Vitest
- 组件测试：Vue Test Utils
- E2E 测试：Playwright

### 4. 错误处理
- 全局错误边界：ErrorBoundary.vue
- API 错误处理：http.js 拦截器
- 用户友好提示：Notification 系统

## 部署指南

### 1. 开发环境
```bash
npm run dev
```

### 2. 生产构建
```bash
npm run build
```

### 3. Docker 部署
```bash
docker build -t star-citizen-promotion .
docker run -p 3000:3000 star-citizen-promotion
```

## 故障排查

### 1. 服务器无法访问
- 检查端口是否被占用：`netstat -ano | findstr :3000`
- 检查防火墙设置
- 检查 Vite 配置：`vite.config.js`

### 2. 组件无法渲染
- 检查组件是否正确导入
- 检查组件名称是否一致
- 检查控制台错误信息

### 3. 状态管理异常
- 检查 Store 是否正确注册
- 检查状态更新是否在正确时机
- 使用 Vue DevTools 调试

## 总结

本架构设计遵循以下原则：
- **模块化**：易于理解和维护
- **可拓展**：易于添加新功能
- **高性能**：优化加载速度和运行效率
- **可测试**：便于编写单元测试
- **可维护**：清晰的代码结构和文档

通过这种设计，可以确保项目在未来持续迭代中保持高质量和可持续性。
