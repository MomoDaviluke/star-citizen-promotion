# 技术线路图（详细解构版）

> **项目**: Star Citizen 战队宣传网站（Stellar Nexus）
> **创建日期**: 2026-08-30
> **当前基线**: v1.6.5（2026-08-30 修正批次：git 重建后 4 提交 + 复审修正；工作树干净）
> **范围**: 2026-08-30 审计发现的技术债补齐 + AI 路线推进
> **配套文档**: [TECH_ARCHIVE_2026-08-30.md](reports/TECH_ARCHIVE_2026-08-30.md)（技术档案）· [EXECUTION_PLAN_TECHDEBT.md](EXECUTION_PLAN_TECHDEBT.md)（操作手册）
> **执行模式**: 单人串行，禁止并行推进多阶段

---

## 0. 一页总览：依赖链

```
 ┌────────────────────────────────────────────────────────────────┐
 │                    前置条件（外部依赖）                          │
 │  [LLM API Key]  [服务器/域名权限]  [GitHub 网络稳定]            │
 └───────────────┬───────────────────┬──────────────┬─────────────┘
                 │                   │              │
                 ▼                   ▼              ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐   ┌──────────────┐
   │  M1 AI  │→│ M5 GEO  │  │ M2 生产 │→  │ M7 远期演进  │
   │ 部署验证 │  │ Phase2 │  │ 部署准备 │   │ (按需)       │
   └─────────┘  └─────────┘  └─────────┘   └──────────────┘
        │           │             │
        ▼           ▼             ▼
   ┌─────────┐  ┌─────────┐  ┌─────────┐
   │ M6 选舰 │  │ M4 覆盖 │  │ M3 E2E  │
   │ 助手    │  │ 率提升  │  │ 扩展    │
   └─────────┘  └─────────┘  └─────────┘
        ▲
        │（全部依赖的公共基线）
   ┌─────────┐
   │ M0 CRUD │  ✅ 已完成 2026-08-30
   │ 补齐    │
   └─────────┘
```

**一句话线路**：M0（已交付）→ 决策门 G1 → [M1 AI 验证] 与 [M2 生产部署] 双线 → M3/M4 质量加固 → M5/M6 AI 增值 → M7 远期。

---

## 1. 模块解构矩阵（总览）

| 模块 | 名称 | 优先级 | 阶段 | 前置 | 预估 | 状态 |
|:--|:--|:--|:--|:--|:--|:--|
| M0 | Admin CRUD 系统性补齐 | P0 | 阶段一·上线前 | 无 | 0.5 天 | ✅ 2026-08-30 |
| M1 | AI-DEP-1/2 部署验证 | P1 | 阶段一·上线前 | **LLM API Key** | 0.5~1 天 | 📋 排队 |
| M2 | 生产部署准备 | P0 | 阶段一·上线前 | **服务器/域名** | 1~2 天 | 📋 排队 |
| M3 | E2E 扩展 | P2 | 阶段二·质量加固 | M0 | 1 天 | ✅ 已完成 2026-08-30 |
| M4 | 前端覆盖率 →70% | P2 | 阶段二·质量加固 | 无 | 3~5 天 | 📋 排队 |
| M5 | AI Phase 2: GEO | P2 | 阶段三·AI 增值 | M1 | 3 天 | 📋 规划 |
| M6 | AI Phase 3: 选舰助手 | P2 | 阶段三·AI 增值 | M1 | 1 周 | 📋 规划 |
| M7 | 远期演进 | P3 | 阶段四·按需 | 生产部署后 | — | ⚪ 远期 |

**阶段划分**：
- **阶段一（上线前闭环）**：M0 ✅ → G1 → M1 → M2 —— 目标是"能上线 + AI 链路真实可用"
- **阶段二（质量加固）**：M3 → M4 —— 目标是"测试防线补全 + 覆盖率门禁上调"
- **阶段三（AI 增值）**：M5 → M6 —— 目标是"GEO 引流 + 选舰转化"
- **阶段四（按需演进）**：M7 —— 软删除/TS 迁移/i18n/Grafana

---

## 2. 决策门（Gate）定义

| 决策门 | 位置 | 判定条件 | 不通过时的动作 |
|:--|:--|:--|:--|
| **G1** | M0 完成 → M1/M2 启动前 | ① 前后端测试全绿 ② typecheck 0 ③ build 通过 ④ 工作树干净 | 修复至全绿再前进（当前已满足） |
| **G2** | M1 完成 → M5/M6 启动前 | ① `ai:ingest` 幂等入库 ② `/ai/health` 200 ③ SSE 流式真实返回 | 不启动 M5/M6；降级链保持 disabled 不阻断 |
| **G3** | M2 完成 → 上线发布前 | ① 生产全服务健康 ② https 可达 ③ 备份恢复演练通过 ④ 告警 webhook 配好 | 不发布；按 M2 清单逐项整改 |
| **G4** | M4 每次门禁上调前 | 覆盖率实测 ≥ 目标值 + 测试无回归 | 不上调门禁，继续补测 |

---

## 3. 模块详细解构

### 3.1 M0 — Admin CRUD 系统性补齐（✅ 已完成 2026-08-30）

**目标**：修复 4 文件 6 处 `/admin/*/new` 死链；成员管理补成可用 CRUD。

| 子任务 | 内容 | 交付文件 | 验收 |
|:--|:--|:--|:--|
| M0-1 | 测试先行（TDD 红） | `tests/views/admin/MembersAdmin.test.js`（2→9 用例） | 新用例先失败 |
| M0-2 | MembersAdmin 弹窗实现 | `src/views/admin/MembersAdmin.vue` | 9/9 绿 |
| M0-3 | Projects/Pilots/Dashboard 死链修复 | 3 个 vue 文件 | 死链 grep 清零 |
| M0-4 | 验证提交 | — | 411/411、typecheck 0、build 过 |

**遗留关联**：M3-2（admin E2E）将验收 M0 的端到端效果。

---

### 3.2 M1 — AI-DEP-1/2 部署验证（P1，排队）

**目标**：让 AI 链路从"代码就绪"变为"真实可用"——知识库入库 + 生产环境变量注入。

**阻塞点**：需要任一 LLM Provider key（豆包 `DOUBAO_API_KEY` / DeepSeek / Anthropic）。

| 子任务 | 动作 | 验证命令 | 预期结果 |
|:--|:--|:--|:--|
| M1-1 | 配置 LLM key 到 `server/.env` | — | — |
| M1-2 | Redis + pgvector 容器运行 | `docker ps` | 两个容器 Up |
| M1-3 | pgvector 建表 | `cd server && npm run ai:migrate` | 0 错误 |
| M1-4 | 5 类知识源入库 | `cd server && npm run ai:ingest` | 入库成功；二次运行幂等 |
| M1-5 | 健康/检索验证 | `curl /api/v1/ai/health`、`/retrieve` | 200 + 真实检索结果 |
| M1-6 | 前端 SSE 流式验证 | 首页 AI 终端发消息 | 流式返回非"服务不可用" |
| M1-7 | 生产 env 注入 | docker-compose | PGVECTOR_URL/REDIS_URL/LLM keys 齐全 |

**风险**：
- R1: 无 key → 无法验证，模块保持挂起（降级链自动 disabled，不阻断启动）
- R2: 双库/双 Redis 漂移（记忆 ENV-01）→ 验证前先确认容器实际端口

---

### 3.3 M2 — 生产部署准备（P0，排队）

**目标**：从"开发可跑"到"生产可上线"。

**阻塞点**：服务器 + 域名 + SSL 证书权限。

| 子任务 | 动作 | 验收 |
|:--|:--|:--|
| M2-1 | docker-compose production profile 激活（保留 127.0.0.1 收紧） | `compose config` 校验通过 |
| M2-2 | nginx.conf.tmpl 域名/SSL 路径核对 | 配置无占位符 |
| M2-3 | 生产 env 清单填写（JWT_SECRET≥32/DB_*/REDIS/PGVECTOR/LLM/WEBHOOK） | 清单齐全 |
| M2-4 | 备份恢复上线前复演 | mysqldump→恢复→COUNT(*) 一致 |

**风险**：
- R3: 需外部权限，无法在本地沙箱完成 → 提供清单，由用户执行
- R4: certbot 续期链路需上线后观察（已有 certbot/ 配置，未实测）

---

### 3.4 M3 — E2E 扩展（P2，✅ 已完成 2026-08-30，commit 976bcf4）

**目标**：E2E spec 7 → 9，补齐 fleet 与 admin 管理链路。

| 子任务 | 内容 | 验收 |
|:--|:--|:--|
| M3-1 | `e2e/fleet.spec.js`：船队列表 + 筛选 + 详情跳转 | 断言基于真实 DOM（FE-10） |
| M3-2 | `e2e/admin.spec.js`：登录 → 成员新增/编辑/删除往返 | 完整 CRUD 往返断言（验收 M0） |

**风险**：
- R5: E2E 需 dev 服务运行 + admin 测试凭据 → 需确认测试账号策略
- R6: 恒真断言陷阱（TD-18 已修，新增用例须遵循）

---

### 3.5 M4 — 前端覆盖率 50.3% → 70%（P2，排队）

**2026-08-30 复审实测基线更新**：语句 56.57% / 行 57.5% / 分支 54.64% / 函数 51.29%（M0/M3 新测试已抬升基线，文档旧值 50.3% 过时）。

**M4-1 攻击顺序（coverage-final.json 按未覆盖语句排序产出）**：

| 批次 | 目标文件 | 未覆盖语句 | 理由 |
|:--|:--|:--|:--|
| 第一批（逻辑层，ROI 最高） | `services/dataService.js`(70) / `router/index.js`(33, 认证守卫) / `stores/calendar.js`(39) / `services/calendarService.js`(20) / `services/monitorService.js`(20) | ~182 | 路线图既定策略：Store/Service/Router 优先 |
| 第二批（转化/用户流） | `views/Join.vue`(69, 招募转化核心) / `views/Calendar.vue`(92) / `views/admin/ApplicationsAdmin.vue`(36) / `views/Profile.vue`(28) / `views/Register.vue`(28) | ~253 | 用户可见路径，E2E 之外的单测纵深 |
| 明确不追 | `planet/rendering/*`（Mars/TexturedSphere/ProceduralMars 等 5 文件 ~477 语句 0%） | — | WebGL 渲染代码，R7：UI 纯渲染测试 ROI 低；若门禁上调受阻可评估 exclude（需单独决策，不默认排除） |

| 子任务 | 动作 | 验收 |
|:--|:--|:--|
| M4-1 | ~~覆盖率报告产出~~ ✅ 2026-08-30（本表） | 报告产出 |
| M4-2 | ~~第一批逻辑层补测~~ ✅ 2026-08-30（dataService API 分支 / 真实路由器守卫 / monitor+calendarService 错误分支 / calendar store 盲区 / BaseModal+BaseTooltip 摆设测试重写；+126 用例，前端 413→509） | ✅ |
| M4-3 | ~~门禁上调 49→55~~ ✅ 2026-08-30（实测 62.03/63.01/58.33/56.33 四项过线）→ 下一档 60 | 每步测试无回归 |

**风险**：
- R7: UI 纯渲染测试 ROI 低 → 按计划只补逻辑，不追 100%
- R8: vitest coverage 目录清理被沙箱拦截 → 用 `--coverage.reportsDirectory` 临时目录（DBG-23）

---

### 3.6 M5 — AI Phase 2: GEO 优化（P2，规划）

**目标**：让 AI 爬虫与生成式搜索能找到并理解站点（GEO = Generative Engine Optimization）。

| # | 动作 | 交付物 | 前置 |
|:--|:--|:--|:--|
| 1 | `public/llms.txt` | AI 爬虫友好的公会信息声明 | 无 |
| 2 | `robots.txt` 放行 AI 爬虫 | GPTBot / ClaudeBot / Bytespider | 无 |
| 3 | Schema.org JSON-LD | Organization / VideoGame / Event / FAQPage | 无 |
| 4 | FAQ 内容入库 | 复用 Phase 0 知识库 | **M1** |
| 5 | 知识图谱 JSON-LD | `/api/v1/geo/graph` | **M1** |
| 6 | 站长平台提交 | 豆包/DeepSeek/Bing Webmaster | 上线后 |

**风险**：
- R9: GEO 是播种性工作、确定性低（RICE 0.17 缓办项）→ 本模块在 M1 验证通过后择机启动
- R10: SPA 预渲染是 GEO 前提 → 已交付（2026-08-24），无需再动

---

### 3.7 M6 — AI Phase 3: 选舰助手（P2，规划）

**目标**：3-5 问 → 画像向量 → pgvector 检索 ships → 规则过滤 → LLM 流式推荐。

| # | 动作 | 交付物 |
|:--|:--|:--|
| 1 | 推荐服务 | `POST /api/v1/ai/recommend/ships` |
| 2 | 前端问卷 + 流式结果 | 选舰组件 |
| 3 | 测试 | 后端单测 + 前端组件测试 + E2E |

**风险**：
- R11: 依赖 M1（key 验证）→ 未验证前不启动
- R12: 动态首页个性化已砍（RICE 0.10）→ 不做

---

### 3.8 M7 — 远期演进（P3）

| 项 | 触发条件 | 工作量 |
|:--|:--|:--|
| MON-F1~F3 多实例聚合/告警静默/趋势报表 | 生产部署后评估 | 2~3 天 |
| 软删除（核心表 deleted_at） | 用户误删反馈 | 2~3 天 |
| 前端 TS 迁移 | 第二开发者加入 | 2~3 周 |
| i18n | 面向国际玩家 | 1~2 周 |
| Grafana 监控仪表盘 | 生产部署后 | 1~2 天 |
| RUM 深化 | 用户量起来 | — |

---

## 4. 版本与里程碑映射

| 版本 | 关键交付 | 对应模块 | 状态 |
|:--|:--|:--|:--|
| v1.6.4 | 监控模块评审闭环 | — | ✅ 2026-08-30 |
| v1.6.5 | Admin CRUD 补齐（M0）+ E2E 扩展 7→9（M3，连带 TD-25/26/27、E2E-SW-01）+ 复审修正批次（TD-28 测试环境加固 / TD-29 版本对齐 / TD-30 文档失真修正） | M0 + M3 | ✅ 2026-08-30（`b9018aa` → RX 批次） |
| v1.7.0 | AI 部署验证 + 生产部署 | M1 + M2 | 📋 计划 |
| v1.8.0 | E2E 扩展 + 覆盖率 70% | M3 + M4 | 📋 计划 |
| v1.9.0 | GEO 搜索优化 | M5 | 📋 规划 |
| v2.0.0 | AI 选舰助手 | M6 | 📋 规划 |

---

## 5. 风险矩阵

| ID | 风险 | 概率 | 影响 | 缓解 |
|:--|:--|:--|:--|:--|
| R1 | 无 LLM key，AI 验证挂起 | 高 | 中 | 降级链自动 disabled；M5/M6 顺延 |
| R3 | 生产部署需外部权限 | 高 | 高 | 输出操作清单交用户执行 |
| R5 | E2E 需测试凭据 | 中 | 低 | 复用 auth.spec 的登录策略 |
| R7 | UI 测试 ROI 低 | 中 | 低 | 只补逻辑，不追 100% |
| R9 | GEO 确定性低 | 中 | 中 | RICE 排序，M1 后择机 |
| **R13** | **git 仓库再损坏**（本轮已发生） | 低 | **高** | 定期 push 远程 + `C:\Users\Administrator\git-recovery\` 备份保留 |
| **R15** | 单测 stub 掩盖真实渲染问题（TD-26 双重布局致 admin 页面空白，单测全绿） | 中 | 高 | 关键用户路径必须有 E2E 覆盖，不能只依赖 stub 单测 |
| R14 | 双 MySQL 库漂移（3306/3307） | 中 | 高 | 启动前确认运行进程实际连接端口（DBG-19） |

---

## 6. 质量门禁（贯穿所有模块）

| 指标 | 当前值 | 目标 | 验证命令 |
|:--|:--|:--|:--|
| 前端测试 | 45 套件/411 用例 | 不回退 | `npm test` |
| 后端测试 | 58 套件/656 用例 | 不回退 | `cd server && npm test` |
| typecheck | 0 错误 | 0 | `npm run typecheck` / `cd server && npm run typecheck` |
| lint | 0 errors | 0 | `npm run lint` |
| CSS 变量 lint | 0 断裂 | 0 | `node scripts/css-var-lint.mjs . --strict` |
| E2E | 7 spec | ≥9（M3 后） | `npm run test:e2e` |
| 前端覆盖率 | 门禁 49/42/42/47 | 70%（M4 后） | `npm run test:coverage` |

---

## 7. 立即行动项（下一步）

| 顺序 | 动作 | 归属 |
|:--|:--|:--|
| 1 | ~~M3（E2E fleet/admin）~~ ✅ 已完成（commit 976bcf4） | AI |
| 2 | RX 修正批次（2026-08-30 复审产出）：W1 git push（R13）→ TD-28 测试环境加固 → TD-29 版本对齐 → TD-30 文档修正 | AI |
| 3 | 用户提供任一 LLM key → 启动 M1 | 用户 |
| 4 | 用户提供服务器/域名 → 启动 M2 | 用户 |
| 5 | M4（覆盖率提升）本地无阻塞，RX 后立即启动 | AI |
| 6 | 每完成一个模块：commit + push（R13 缓解）+ 更新本线路图状态列 | AI |

> **注意**：R13（git 损坏）教训——建议每次模块完成后 `git push` 到远程（当前网络对 GitHub 大对象不稳，小提交 push 通常可行），避免本地历史再丢失。
