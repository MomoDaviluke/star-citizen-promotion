# 上线修复清单

> 生成日期：2026-05-10  
> 最后更新：2026-05-10  
> 目的：列出所有阻碍上线的未完成功能，按优先级排序，逐项修复

---

## P0 — 阻断上线 ✅ 已完成

### 1. 舰队模块后端 API ✅

**已创建**：
- `server/src/services/fleetService.ts` — 飞船 CRUD + 统计服务
- `server/src/routes/fleet.ts` — 6 个端点（GET /stats, GET /, GET /:id, POST /, PATCH /:id, DELETE /:id）
- `server/src/database/init.ts` — 增加 `ships` 表
- `server/src/index.ts` — 注册 `/api/fleet` 路由

---

### 2. 活动日历模块后端 API ✅

**已创建**：
- `server/src/services/eventService.ts` — 活动 CRUD + 报名/取消 + ICS 导出
- `server/src/routes/events.ts` — 8 个端点（含 join/leave/export/ics）
- `server/src/database/init.ts` — 增加 `events` + `event_participants` 表
- `server/src/index.ts` — 注册 `/api/events` 路由

---

### 3. 管理后台功能实现 ✅

**后端新增**：
- `server/src/routes/settings.ts` — 站点设置 CRUD
- `server/src/routes/admin.ts` — 管理员操作（reset-db / clear-cache）
- `server/src/database/init.ts` — 增加 `settings` 表

**前端修复**：

| 文件 | 函数 | 状态 |
|------|------|------|
| `MembersAdmin.vue` | `editMember()` | ✅ 已实现编辑弹窗 + PUT 调用 |
| `ProjectsAdmin.vue` | `editProject()` | ✅ 已实现编辑弹窗 + PUT 调用 |
| `PilotsAdmin.vue` | `editPilot()` | ✅ 已实现编辑弹窗 + PUT 调用 |
| `Settings.vue` | `saveSiteSettings()` | ✅ 已调用 PUT /api/settings |
| `Settings.vue` | `resetDatabase()` | ✅ 已调用 POST /api/admin/reset-db |
| `Settings.vue` | `clearCache()` | ✅ 已调用 POST /api/admin/clear-cache |
| `dataService.js` | `submitApplication()` | ✅ API 模式下正常工作 |
| `Dashboard.vue` | activityLogs | ✅ 已从 API 加载活动日志 |
| `calendar.js` | `leaveEvent` | ✅ 已补充缺失的 action |

---

### 4. 前端测试覆盖 ✅

**已创建 19 个测试文件**：

```
# 视图测试 (7)
tests/views/Login.test.js
tests/views/Register.test.js
tests/views/Profile.test.js
tests/views/Join.test.js
tests/views/ApplicationStatus.test.js
tests/views/Fleet.test.js
tests/views/Calendar.test.js

# 管理后台测试 (3)
tests/views/admin/Dashboard.test.js
tests/views/admin/MembersAdmin.test.js
tests/views/admin/ApplicationsAdmin.test.js

# 公共组件测试 (5)
tests/components/common/BaseButton.test.js
tests/components/common/BaseCard.test.js
tests/components/common/BaseModal.test.js
tests/components/common/BaseBadge.test.js
tests/components/common/BaseTooltip.test.js

# 组合式函数测试 (4)
tests/composables/useParallax.test.js
tests/composables/useScrollReveal.test.js
tests/composables/useSoundEffect.test.js
tests/composables/useWebSocket.test.js
```

**测试结果**：30/31 文件通过，173/183 测试通过（10 个失败为 pre-existing authService 测试）

---

## P1 — 高优先级（待修复）

### 5. E2E 测试覆盖不足

**问题**：20 个路由页面只有 2 个 E2E 测试（`e2e/home.spec.js`、`e2e/join.spec.js`）。

**至少需要补充**：
- `e2e/login.spec.js` — 登录流程
- `e2e/register.spec.js` — 注册流程
- `e2e/profile.spec.js` — 个人中心
- `e2e/admin-dashboard.spec.js` — 管理后台核心流程
- `e2e/fleet.spec.js` — 舰队浏览
- `e2e/calendar.spec.js` — 日历查看

**预计工作量**：3-4 小时

---

### 6. console.log 清理

**前端必须清理**：

| 文件 | 行号 | 内容 |
|------|------|------|
| `src/composables/useWebSocket.js` | 75, 103, 108 | WebSocket 连接日志 |
| `src/services/dataService.js` | 174 | `console.log('模拟提交申请:', data)` |
| `src/services/wsService.js` | 197 | WebSocket 重连日志 |

后端 `server/src/index.ts`、`server/src/database/*.ts` 中的启动日志可保留，但建议改为结构化日志。

**预计工作量**：30 分钟

---

### 7. 站点配置文件填入真实值

**文件**：`src/config/site.config.js`

```diff
- author: 'Your Team Name'
+ author: '真实团队名称'

- email: 'team@example.com'
+ email: '真实联系邮箱'

- discord: 'your-discord-invite'
+ discord: '真实 Discord 邀请链接'

- qqGroup: '123456789'
+ qqGroup: '真实 QQ 群号'

- github: 'https://github.com/your-org'
+ github: '真实 GitHub 地址'
```

**预计工作量**：5 分钟

---

## P2 — 中优先级（待修复）

### 8. AI 服务：保留还是移除？

**问题**：`src/services/AIService.js` 实现了完整的任务队列、并发控制、重试机制，Vite 配置代理 `/ai → localhost:3002`，但 AI 服务端从未实现。

**建议**：若 MVP 不需要 AI，移除前端引用和代理配置；若需要，需实现 Python/Node AI 服务。

**预计工作量**：移除 15 分钟 / 实现待定

---

### 9. 数据库迁移方案改进

**问题**：当前数据库表结构通过 `server/src/database/init.ts` 在启动时自动创建，没有独立的 `.sql` 迁移文件。生产环境建议版本化迁移。

**建议**：引入 knex 或保持现状（代码内迁移可正常工作）。

**预计工作量**：1-2 小时

---

### 10. 项目元信息补充

**文件**：`package.json`

```json
"repository": { "type": "git", "url": "" },   // 填入仓库地址
"bugs": { "url": "" },                          // 填入 Issues 地址
"homepage": ""                                  // 填入网站地址
```

**预计工作量**：2 分钟

---

## 建议执行顺序（下次）

```
第1步 → 站点配置填入真实值（5分钟）
第2步 → console.log 清理（30分钟）
第3步 → 补充 E2E 测试（3-4小时）
第4步 → 项目元信息 + AI 服务决策（15分钟）
第5步 → 数据库迁移方案改进（可选，1-2小时）
```

**剩余预估**：约 4-8 小时

---

## 完成标准

- [x] 后端 API 覆盖所有前端页面（Fleet / Calendar / Settings）
- [x] 管理后台编辑/设置功能真实可用
- [x] 前端测试覆盖率从 0 到有基线覆盖
- [ ] E2E 测试覆盖核心流程（登录、注册、管理后台）
- [ ] 前端生产代码无 console.log
- [ ] 站点配置无占位符
- [ ] 当前未提交文件整理提交
