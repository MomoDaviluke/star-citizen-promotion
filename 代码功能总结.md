# 代码功能总结

## 项目概述

本项目是一个基于 **Vue 3 + Vite** 前端和 **Express + TypeScript + MySQL** 后端的星际公民战队宣传网站。项目采用前后端分离架构，提供战队信息展示、成员管理、项目追踪、入队申请、飞行员档案等核心功能，并配备完整的管理后台。

---

## 一、整体架构说明

### 1.1 技术栈架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Frontend)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Vue 3     │  │  Vue Router │  │      Pinia          │  │
│  │  (组合式API) │  │   (路由)     │  │    (状态管理)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Axios     │  │  自定义HTTP   │  │    组件化架构        │  │
│  │  (HTTP请求)  │  │   客户端     │  │   (原子化设计)       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ HTTP / WebSocket
┌─────────────────────────────────────────────────────────────┐
│                        后端层 (Backend)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │   Express   │  │  TypeScript │  │     MySQL 2         │  │
│  │   (Web框架)  │  │   (类型安全) │  │   (数据库驱动)       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │    JWT      │  │   bcryptjs  │  │      Winston        │  │
│  │  (认证令牌)  │  │  (密码加密)  │  │    (日志记录)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │    WebSocket│  │  Prometheus │  │      Swagger        │  │
│  │   (实时通信) │  │   (监控指标) │  │    (API文档)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼ SQL
┌─────────────────────────────────────────────────────────────┐
│                      数据层 (Database)                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │    MySQL    │  │  连接池管理  │  │    事务支持          │  │
│  │  (关系型DB)  │  │  (性能优化)  │  │   (数据一致性)       │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 目录结构

```
star-citizen-promotion/
├── src/                          # 前端源代码
│   ├── main.js                   # 应用入口：全局配置、插件初始化
│   ├── App.vue                   # 根组件：布局框架、全局状态
│   ├── router/                   # 路由配置
│   │   └── index.js              # 路由定义、导航守卫、权限控制
│   ├── services/                 # 服务层
│   │   ├── http.js               # HTTP客户端：拦截器、错误处理
│   │   ├── auth.js               # 认证服务：登录、注册、令牌管理
│   │   └── data.js               # 数据服务：CRUD操作封装
│   ├── components/               # 组件库
│   │   ├── common/               # 通用组件
│   │   │   ├── PageTitle.vue     # 页面标题组件
│   │   │   ├── LoadingState.vue  # 加载状态组件
│   │   │   └── ErrorBoundary.vue # 错误边界组件
│   │   ├── layout/               # 布局组件
│   │   │   ├── AppHeader.vue     # 顶部导航
│   │   │   ├── AppFooter.vue     # 页脚
│   │   │   └── AppSidebar.vue    # 侧边栏
│   │   └── sections/             # 页面区块组件
│   │       ├── HeroSection.vue   # 首页英雄区
│   │       ├── StatsSection.vue  # 统计数据区
│   │       └── MembersSection.vue# 成员展示区
│   ├── views/                    # 页面视图
│   │   ├── Home.vue              # 首页
│   │   ├── About.vue             # 关于我们
│   │   ├── Members.vue           # 成员列表
│   │   ├── Projects.vue          # 项目展示
│   │   ├── Pilots.vue            # 飞行员档案
│   │   ├── Join.vue              # 加入申请
│   │   ├── Login.vue             # 登录页
│   │   ├── Register.vue          # 注册页
│   │   ├── Profile.vue           # 个人中心
│   │   ├── ApplicationStatus.vue # 申请状态查询
│   │   └── admin/                # 管理后台
│   │       ├── Dashboard.vue     # 数据概览
│   │       ├── AdminLayout.vue   # 后台布局
│   │       ├── MembersAdmin.vue  # 成员管理
│   │       ├── ProjectsAdmin.vue # 项目管理
│   │       ├── ApplicationsAdmin.vue # 申请审核
│   │       └── PilotsAdmin.vue   # 飞行员管理
│   └── styles/                   # 样式文件
│       └── variables.css         # CSS变量定义
├── server/                       # 后端源代码
│   ├── src/
│   │   ├── index.ts              # 服务入口：Express实例、中间件挂载
│   │   ├── config/               # 配置管理
│   │   │   └── index.ts          # 环境变量、配置校验
│   │   ├── database/             # 数据库层
│   │   │   ├── pool.ts           # 连接池管理
│   │   │   ├── init.ts           # 数据库初始化
│   │   │   ├── migrate.ts        # 迁移脚本
│   │   │   └── seed.ts           # 种子数据
│   │   ├── middleware/           # 中间件
│   │   │   ├── auth.ts           # 认证中间件
│   │   │   ├── errorHandler.ts   # 错误处理
│   │   │   ├── requestLogger.ts  # 请求日志
│   │   │   ├── requestId.ts      # 请求追踪
│   │   │   ├── auditLogger.ts    # 审计日志
│   │   │   ├── pagination.ts     # 分页解析
│   │   │   └── metrics.ts        # 监控指标
│   │   ├── routes/               # 路由层
│   │   │   ├── auth.ts           # 认证路由
│   │   │   ├── members.ts        # 成员路由
│   │   │   ├── projects.ts       # 项目路由
│   │   │   ├── pilots.ts         # 飞行员路由
│   │   │   ├── applications.ts   # 申请路由
│   │   │   └── stats.ts          # 统计路由
│   │   ├── services/             # 业务服务层
│   │   │   ├── authService.ts    # 认证业务
│   │   │   ├── memberService.ts  # 成员业务
│   │   │   ├── projectService.ts # 项目业务
│   │   │   ├── pilotService.ts   # 飞行员业务
│   │   │   └── applicationService.ts # 申请业务
│   │   ├── utils/                # 工具函数
│   │   │   ├── logger.ts         # Winston日志
│   │   │   └── jwt.ts            # JWT工具
│   │   └── websocket.ts          # WebSocket服务
│   └── package.json
├── vite.config.js                # Vite构建配置
└── package.json
```

---

## 二、核心功能模块介绍

### 2.1 前端核心模块

#### 2.1.1 路由系统 (`src/router/index.js`)

**功能**：管理应用页面导航和访问控制

**核心逻辑**：
- 使用 Vue Router 4 创建路由实例
- 定义公开路由（首页、关于、登录、注册等）和受保护路由（管理后台）
- 导航守卫 `beforeEach` 检查用户认证状态
- 未认证用户访问受保护路由时重定向到登录页
- 支持路由元信息配置（标题、权限要求）

**设计思路**：
- 采用路由懒加载优化首屏性能
- 路由配置集中管理，便于维护
- 导航守卫统一处理权限校验，避免重复代码

#### 2.1.2 HTTP客户端 (`src/services/http.js`)

**功能**：封装 Axios，提供统一的 HTTP 请求处理和错误管理

**核心逻辑**：
- 创建 Axios 实例，配置基础 URL 和超时时间
- 请求拦截器：自动附加 JWT 认证令牌到请求头
- 响应拦截器：统一处理 401 未授权错误（自动跳转登录页）
- 错误处理：将 HTTP 错误转换为友好的错误消息

**设计思路**：
- 拦截器模式实现横切关注点（认证、错误处理）
- 与服务层解耦，便于替换底层 HTTP 库
- 统一的错误处理提升用户体验

#### 2.1.3 认证服务 (`src/services/auth.js`)

**功能**：管理用户认证状态，提供登录、注册、登出功能

**核心逻辑**：
- `login(credentials)`：调用后端登录接口，存储 JWT 令牌和用户信息到 localStorage
- `register(data)`：调用后端注册接口
- `logout()`：清除本地存储的认证信息
- `getUser()`：从 localStorage 获取当前用户信息
- `isAuthenticated()`：检查用户是否已登录
- `updateProfile(data)`：更新用户资料
- `changePassword(data)`：修改密码

**设计思路**：
- 使用 localStorage 持久化认证状态，支持页面刷新后保持登录
- 服务层封装 API 调用细节，视图层只需关注业务逻辑
- 提供响应式用户信息，支持组件间共享认证状态

#### 2.1.4 数据服务 (`src/services/data.js`)

**功能**：封装所有后端数据交互，提供 CRUD 操作

**核心方法**：
- 成员管理：`getMembers()`, `getMember(id)`, `createMember()`, `updateMember()`, `deleteMember()`
- 项目管理：`getProjects()`, `getProject(id)`, `createProject()`, `updateProject()`, `deleteProject()`
- 飞行员管理：`getPilots()`, `getPilot(id)`, `createPilot()`, `updatePilot()`, `deletePilot()`
- 申请管理：`getApplications()`, `submitApplication()`, `updateApplicationStatus()`
- 统计查询：`getStats()`, `getApplicationByEmail()`

**设计思路**：
- 按业务领域组织 API 方法，代码结构清晰
- 统一返回 `{ success, data, error }` 格式，便于错误处理
- 支持分页参数，适配后端分页接口

### 2.2 后端核心模块

#### 2.2.1 认证服务层 (`server/src/services/authService.ts`)

**功能**：实现用户注册、登录、资料管理、密码修改等核心业务逻辑

**核心逻辑**：
- `registerUser()`：在数据库事务中检查用户名/邮箱唯一性，使用 bcrypt 加密密码，生成 JWT 令牌
- `loginUser()`：验证邮箱和密码，返回用户信息和 JWT 令牌
- `getUserById()`：通过 ID 查询用户信息
- `updateUserProfile()`：在事务中更新用户资料，检查用户名唯一性
- `changePassword()`：验证当前密码，使用 bcrypt 加密新密码
- `refreshUserToken()`：验证现有令牌并签发新令牌

**设计思路**：
- 数据库事务保护关键操作（注册、资料更新、密码修改），确保数据一致性
- bcrypt 密码加密（默认 12 轮 salt），保障密码安全
- JWT 令牌设置过期时间（默认 7 天），平衡安全性和用户体验
- 自定义 ApiError 类统一错误处理，支持不同 HTTP 状态码

#### 2.2.2 成员服务层 (`server/src/services/memberService.ts`)

**功能**：实现团队成员的增删改查和分页查询

**核心逻辑**：
- `getMembers()`：支持状态筛选和分页，返回成员列表和分页信息
- `getMemberById()`：通过 ID 查询单个成员
- `createMember()`：创建新成员，使用 UUID 作为主键
- `updateMember()`：在事务中更新成员信息，动态构建 UPDATE 语句
- `deleteMember()`：删除成员前检查是否存在

**设计思路**：
- 动态 SQL 构建实现灵活的字段更新，避免未提供字段被覆盖
- 白名单机制控制可更新字段，防止非法字段更新
- 分页查询返回 `hasMore` 标志，便于前端实现无限滚动

#### 2.2.3 项目服务层 (`server/src/services/projectService.ts`)

**功能**：实现战队项目的增删改查和状态管理

**核心逻辑**：
- `getProjects()`：支持状态筛选和分页查询
- `getProjectById()`：通过 ID 查询项目详情
- `createProject()`：创建新项目，默认状态为 planning，进度为 0
- `updateProject()`：在事务中更新项目信息
- `deleteProject()`：删除项目前检查是否存在

**设计思路**：
- 与成员服务采用相同的架构模式，保持代码一致性
- 支持项目状态（planning/active/completed/cancelled）和进度追踪

#### 2.2.4 申请服务层 (`server/src/services/applicationService.ts`)

**功能**：实现入队申请的提交、查询和审核流程

**核心逻辑**：
- `getApplications()`：支持状态筛选和分页，关联查询审核人信息
- `getApplicationById()`：通过 ID 查询申请详情
- `submitApplication()`：在事务中检查 24 小时内是否重复提交，防止重复申请
- `updateApplicationStatus()`：在事务中更新申请状态，记录审核人和审核时间
- `deleteApplication()`：删除申请前检查是否存在

**设计思路**：
- 24 小时重复提交检查，避免用户频繁提交
- 审核流程记录审核人和时间，支持审计追踪
- 关联查询用户表获取审核人姓名，提升查询效率

#### 2.2.5 飞行员服务层 (`server/src/services/pilotService.ts`)

**功能**：实现飞行员档案的增删改查，支持按任务数排序

**核心逻辑**：
- `getPilots()`：支持状态筛选，按任务数降序排列
- `getPilotById()`：通过 ID 查询飞行员详情
- `createPilot()`：创建新飞行员档案
- `updatePilot()`：在事务中更新飞行员信息
- `deletePilot()`：删除飞行员前检查是否存在

**设计思路**：
- 按任务数排序展示王牌飞行员，突出核心成员
- 支持飞行员状态（active/inactive/kia），灵活管理

### 2.3 数据库模块

#### 2.3.1 连接池管理 (`server/src/database/pool.ts`)

**功能**：管理 MySQL 数据库连接池，提供统一的数据库访问接口

**核心逻辑**：
- `createPool()`：根据环境配置创建连接池，生产环境默认 20 连接，开发环境 10 连接
- `getPool()`：获取连接池实例，未初始化时抛出错误
- `query()`：执行 SQL 查询，返回结果数组
- `queryOne()`：执行单条查询，返回单个对象或 null
- `insert()` / `update()` / `remove()`：封装 INSERT/UPDATE/DELETE 操作，返回影响行数
- `transaction()`：执行数据库事务，自动处理提交和回滚
- `testConnection()`：测试数据库连接
- `closePool()`：优雅关闭连接池

**设计思路**：
- 连接池复用数据库连接，减少连接开销
- 事务回调函数模式简化事务管理，自动处理回滚
- 参数化查询防止 SQL 注入攻击

#### 2.3.2 数据库初始化 (`server/src/database/init.ts`)

**功能**：自动创建数据库表结构和初始数据

**核心逻辑**：
- `createTables()`：创建 users、members、projects、pilots、applications、stats、activity_logs 表
- `seedInitialData()`：检查数据是否存在，不存在则插入种子数据
- 所有表使用 InnoDB 引擎和 utf8mb4 字符集，支持中文和表情符号
- 合理设置索引（status、email、created_at 等），优化查询性能

**设计思路**：
- 表结构使用 IF NOT EXISTS，支持重复执行不报错
- 外键约束确保数据完整性（如 applications.reviewed_by 关联 users.id）
- 种子数据提供初始测试数据，便于开发调试

---

## 三、关键算法概述

### 3.1 JWT 认证流程

```
┌─────────┐                    ┌─────────┐                    ┌─────────┐
│  客户端  │ ── 1.登录请求 ──▶ │  后端   │ ── 2.验证密码 ──▶ │ 数据库  │
│         │                    │         │ ◀─ 3.用户信息 ── │         │
│         │ ◀─ 4.JWT令牌 ─── │         │                    │         │
│         │                    │         │                    │         │
│         │ ── 5.携带令牌 ──▶ │         │ ── 6.验证签名 ──▶ │         │
│         │                    │         │                    │         │
│         │ ◀─ 7.受保护数据 ─ │         │                    │         │
└─────────┘                    └─────────┘                    └─────────┘
```

**算法说明**：
1. 用户提交邮箱和密码
2. 后端查询数据库验证用户存在
3. 使用 bcrypt 比对密码哈希
4. 验证通过后，使用 jwt.sign() 生成令牌（包含 userId、签发者、过期时间）
5. 客户端存储令牌（localStorage），后续请求携带在 Authorization 头中
6. 后端使用 jwt.verify() 验证令牌签名和过期时间
7. 验证通过后，根据 userId 查询用户信息，附加到请求对象

### 3.2 数据库事务处理

```typescript
// 事务执行模式
export async function transaction<T>(
  callback: (connection: PoolConnection) => Promise<T>
): Promise<T> {
  const connection = await getPool().getConnection()
  try {
    await connection.beginTransaction()  // 开启事务
    const result = await callback(connection)  // 执行业务逻辑
    await connection.commit()  // 提交事务
    return result
  } catch (error) {
    await connection.rollback()  // 出错回滚
    throw error
  } finally {
    connection.release()  // 释放连接
  }
}
```

**算法说明**：
- 从连接池获取连接
- 开启事务，确保后续操作原子性
- 执行业务回调函数（可包含多个 SQL 操作）
- 成功则提交事务，失败则回滚所有操作
- 无论成功失败，最终释放连接回连接池

### 3.3 动态 SQL 构建（更新操作）

```typescript
// 动态构建 UPDATE 语句
const allowedColumns = ['name', 'role', 'intro', 'avatar', 'status']
const columnMap: Record<string, unknown> = { name, role, intro, avatar, status }

for (const col of allowedColumns) {
  if (columnMap[col] !== undefined) {
    updates.push(col)
    values.push(columnMap[col])
  }
}

const setClause = updates.map((col) => `${col} = ?`).join(', ')
await conn.execute(`UPDATE members SET ${setClause} WHERE id = ?`, values)
```

**算法说明**：
- 定义允许更新的字段白名单，防止非法字段更新
- 遍历白名单，检查传入数据中哪些字段有值
- 动态构建 SET 子句（`field1 = ?, field2 = ?`）
- 只更新提供的字段，未提供的字段保持原值
- 有效防止 SQL 注入（使用参数化查询）

### 3.4 请求日志脱敏算法

```typescript
function sanitizeBody(body: unknown): unknown {
  if (!body || typeof body !== 'object') return body
  if (Array.isArray(body)) return body.map(item => sanitizeBody(item))

  const sanitized: Record<string, unknown> = {}
  for (const [key, value] of Object.entries(body)) {
    if (isSensitiveField(key)) {
      sanitized[key] = '***REDACTED***'  // 敏感字段脱敏
    } else if (typeof value === 'object' && value !== null) {
      sanitized[key] = sanitizeBody(value)  // 递归处理嵌套对象
    } else {
      sanitized[key] = value  // 非敏感字段保留原值
    }
  }
  return sanitized
}
```

**算法说明**：
- 递归遍历请求体中的所有字段
- 检查字段名是否包含敏感关键词（password、token、secret 等）
- 敏感字段替换为 `***REDACTED***`，避免日志泄露
- 支持嵌套对象和数组的递归脱敏

### 3.5 分页查询算法

```typescript
// 分页参数解析
const page = Math.max(1, parseInt(req.query.page as string, 10) || 1)
const limit = Math.min(maxLimit, Math.max(1, parseInt(req.query.limit as string, 10) || defaultLimit))
const offset = (page - 1) * limit

// 分页查询
sql += ' ORDER BY created_at DESC LIMIT ? OFFSET ?'
params.push(limit, offset)

// 返回分页信息
return {
  members,
  pagination: {
    total,           // 总记录数
    limit,           // 每页条数
    offset,          // 当前偏移量
    hasMore: offset + members.length < total  // 是否还有更多
  }
}
```

**算法说明**：
- 解析并校验分页参数，确保 page >= 1，1 <= limit <= maxLimit
- 计算偏移量 `offset = (page - 1) * limit`
- 使用 SQL LIMIT/OFFSET 实现分页查询
- 单独查询总记录数，计算 hasMore 标志
- 前端可根据 hasMore 实现无限滚动或分页按钮

---

## 四、代码设计思路总结

### 4.1 分层架构设计

项目采用经典的三层架构，实现关注点分离：

| 层级 | 职责 | 代表文件 |
|------|------|----------|
| **表现层** | 处理 HTTP 请求/响应，参数校验，路由分发 | `routes/*.ts`, `middleware/*.ts` |
| **业务层** | 实现核心业务逻辑，事务管理，数据校验 | `services/*Service.ts` |
| **数据层** | 数据库访问，连接池管理，SQL 执行 | `database/pool.ts` |

**设计优势**：
- 各层职责清晰，便于独立开发和测试
- 业务逻辑与 HTTP 细节解耦，便于复用和单元测试
- 数据访问集中管理，便于优化和监控

### 4.2 中间件管道设计

Express 中间件按顺序执行，形成处理管道：

```
请求 → requestId → requestLogger → CORS → 解析Body → 路由处理 → 业务逻辑 → 响应
         ↓              ↓            ↓         ↓           ↓           ↓
      生成追踪ID    记录请求日志   跨域处理   解析JSON    权限校验    数据库操作
```

**核心中间件**：
- `requestId`：为每个请求生成唯一 ID，支持全链路追踪
- `requestLogger`：记录请求详情，自动脱敏敏感字段
- `auth`：验证 JWT 令牌，保护受保护路由
- `errorHandler`：统一错误处理，返回标准化错误响应
- `auditLogger`：记录管理操作审计日志

### 4.3 错误处理策略

采用统一的错误处理机制：

1. **自定义 ApiError 类**：封装 HTTP 状态码和错误信息
2. **工厂方法**：`ApiError.badRequest()`, `ApiError.unauthorized()` 等快速创建错误
3. **全局错误中间件**：捕获所有未处理错误，返回标准化响应
4. **环境适配**：生产环境隐藏详细错误信息，开发环境显示堆栈

```typescript
// 错误响应格式
{
  success: false,
  error: "错误描述",
  errors: null,        // 详细错误信息（可选）
  timestamp: "2024-01-01T00:00:00.000Z"
}
```

### 4.4 安全设计

| 安全措施 | 实现方式 | 相关文件 |
|----------|----------|----------|
| **密码加密** | bcryptjs，12轮salt | `authService.ts` |
| **认证令牌** | JWT，7天过期 | `auth.ts`, `jwt.ts` |
| **SQL注入防护** | 参数化查询 | `pool.ts` |
| **敏感数据脱敏** | 日志字段过滤 | `requestLogger.ts` |
| **配置安全** | 生产环境强制校验 | `config/index.ts` |
| **审计日志** | 自动记录管理操作 | `auditLogger.ts` |

### 4.5 性能优化策略

| 优化点 | 实现方式 | 效果 |
|--------|----------|------|
| **连接池** | MySQL 连接池复用连接 | 减少连接开销 |
| **分页查询** | LIMIT/OFFSET 分页 | 避免大数据量查询 |
| **索引优化** | status、email、created_at 索引 | 加速筛选和排序 |
| **并发请求** | Promise.all 并行请求 | 减少等待时间 |
| **懒加载** | Vue 路由懒加载 | 减少首屏加载 |

### 4.6 可维护性设计

1. **模块化组织**：按功能模块划分目录，代码结构清晰
2. **TypeScript 类型**：定义接口和类型，编译期发现错误
3. **JSDoc 注释**：详细的函数注释，支持 IDE 智能提示
4. **配置集中**：环境变量和配置统一在 `config/index.ts` 管理
5. **日志规范**：结构化日志，支持日志级别控制和文件输出

---

## 五、数据库表结构

### 5.1 用户表 (users)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| username | VARCHAR(50) | 用户名，唯一 |
| email | VARCHAR(100) | 邮箱，唯一 |
| password_hash | VARCHAR(255) | 密码哈希 |
| role | ENUM | 角色：admin/member/guest |
| avatar | VARCHAR(255) | 头像URL |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 5.2 成员表 (members)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(50) | 姓名 |
| role | VARCHAR(50) | 职位 |
| intro | TEXT | 简介 |
| avatar | VARCHAR(255) | 头像URL |
| join_date | DATE | 加入日期 |
| status | ENUM | 状态：active/inactive/retired |

### 5.3 项目表 (projects)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(100) | 项目名称 |
| period | VARCHAR(50) | 周期 |
| description | TEXT | 描述 |
| status | ENUM | 状态：planning/active/completed/cancelled |
| progress | INT | 进度 0-100 |
| participants | INT | 参与人数 |

### 5.4 飞行员表 (pilots)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(50) | 姓名 |
| callsign | VARCHAR(50) | 呼号 |
| ship | VARCHAR(100) | 飞船型号 |
| description | TEXT | 描述 |
| image | VARCHAR(255) | 图片URL |
| missions | INT | 任务数 |
| kills | INT | 击毁数 |
| status | ENUM | 状态：active/inactive/kia |

### 5.5 申请表 (applications)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| name | VARCHAR(50) | 申请人姓名 |
| email | VARCHAR(100) | 联系邮箱 |
| discord | VARCHAR(50) | Discord ID |
| experience | TEXT | 游戏经验 |
| availability | VARCHAR(50) | 在线时间 |
| reason | TEXT | 加入原因 |
| status | ENUM | 状态：pending/approved/rejected |
| reviewed_by | VARCHAR(36) | 审核人ID |
| reviewed_at | TIMESTAMP | 审核时间 |
| note | TEXT | 审核备注 |

---

## 六、API 接口概览

### 6.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| GET | /api/auth/me | 获取当前用户 |
| PUT | /api/auth/profile | 更新资料 |
| PUT | /api/auth/password | 修改密码 |

### 6.2 成员接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/members | 获取成员列表 |
| GET | /api/members/:id | 获取成员详情 |
| POST | /api/members | 创建成员 |
| PUT | /api/members/:id | 更新成员 |
| DELETE | /api/members/:id | 删除成员 |

### 6.3 项目接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/projects | 获取项目列表 |
| GET | /api/projects/:id | 获取项目详情 |
| POST | /api/projects | 创建项目 |
| PUT | /api/projects/:id | 更新项目 |
| DELETE | /api/projects/:id | 删除项目 |

### 6.4 飞行员接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/pilots | 获取飞行员列表 |
| GET | /api/pilots/:id | 获取飞行员详情 |
| POST | /api/pilots | 创建飞行员 |
| PUT | /api/pilots/:id | 更新飞行员 |
| DELETE | /api/pilots/:id | 删除飞行员 |

### 6.5 申请接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/applications | 获取申请列表 |
| POST | /api/applications | 提交申请 |
| PUT | /api/applications/:id/status | 更新申请状态 |
| GET | /api/applications/check | 查询申请状态 |

### 6.6 统计接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/stats | 获取统计数据 |

---

## 七、部署与运行

### 7.1 开发环境

```bash
# 安装依赖
npm install

# 启动前端开发服务器
npm run dev

# 启动后端开发服务器
cd server && npm run dev
```

### 7.2 生产环境

```bash
# 构建前端
npm run build

# 启动后端生产服务器
cd server && npm start
```

### 7.3 环境变量配置

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| NODE_ENV | 运行环境 | development |
| PORT | 后端端口 | 3001 |
| FRONTEND_URL | 前端地址 | http://localhost:3000 |
| JWT_SECRET | JWT密钥 | 必填（生产环境） |
| JWT_EXPIRES_IN | JWT过期时间 | 7d |
| DB_HOST | 数据库主机 | localhost |
| DB_PORT | 数据库端口 | 3306 |
| DB_USER | 数据库用户 | root |
| DB_PASSWORD | 数据库密码 | 必填（生产环境） |
| DB_NAME | 数据库名称 | star_citizen_promotion |

---

## 八、总结

本项目采用现代化的全栈技术栈，实现了完整的战队宣传网站功能。代码架构清晰，分层明确，注重安全性、可维护性和性能优化。通过详细的注释和文档，便于团队成员快速理解和维护代码。

**核心亮点**：
- 完整的用户认证和权限管理
- 数据库事务保护关键业务操作
- 统一的错误处理和日志记录
- 敏感数据自动脱敏
- 生产环境配置安全校验
- 审计日志追踪管理操作
- 响应式设计适配多端设备
